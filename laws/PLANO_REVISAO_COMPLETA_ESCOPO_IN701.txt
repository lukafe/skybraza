================================================================================
PLANO SEQUENCIAL — Revisão completa do código e da lógica de avaliação dos incisos
IN 701 / Res. 520 · trilhas intermediária, custodiante, corretora
================================================================================
Objetivo: auditar o repositório de ponta a ponta, validar a lógica de escopo
(active_keys, obrigatórios, gatilhos, supressões, narrativas, Jornada 2), registar
problemas e melhorias, implementar por prioridade.

Como usar: executar as fases **na ordem**; só avançar quando a fase anterior tiver
ata curta (achados + decisão: corrigir agora / backlog / aceitar risco documentado).

REGISTO DE EXECUÇÃO (atas)
--------------------------------------------------------------------------------
· Fase 1 — 2026-03-27 — laws/REVISAO_EXEC_FASE1_MAPA_SISTEMA.txt (concluída)
· Fase 2 — 2026-03-27 — laws/REVISAO_EXEC_FASE2_MATRIZES.txt (2.1 OK; 2.2–2.3 pendentes)
· Fase 3 — 2026-03-27 — laws/REVISAO_EXEC_FASE3_QUESTIONARIOS.txt (concluída; correção texto «fora»)

--------------------------------------------------------------------------------
FASE 1 — Mapa do sistema e contratos de dados
--------------------------------------------------------------------------------
1.1 Listar entradas e saídas do motor:
      · Entrada: `answers` (dict), `track` (str)
      · Saída: `DataFrame` + `meta` com `active_keys`, `triggered_by`, `mandatory_count`,
        `incisos_sujeitos_auditoria`, `incisos_fora_escopo_auditoria`, `journey_2`, etc.
1.2 Diagramar fluxo em 5 passos fixos:
      (a) `build_mandatory_keys(track)` + `build_incisos_matrix(track)`
      (b) `normalize_answers` + `resolve_triggers`
      (c) `active_keys = mandatory ∪ triggered`
      (d) `suppress_custody_cluster_if_non_custodial`
      (e) narrativas, hints BCB, readiness, `build_journey_2_payload`
1.3 Identificar todos os consumidores de `meta`/`active_keys`:
      `main.py`, `app.py`, `readiness.py`, `evidence_requests.py`, testes, `export_*`.
Entregável: esquema ASCII ou lista de ficheiros com responsabilidade única.

--------------------------------------------------------------------------------
FASE 2 — Matrizes normativas (fonte de verdade dos incisos)
--------------------------------------------------------------------------------
2.1 Por trilha (`COVERAGE_MATRIX.yaml` raiz + `tracks/custodiante` + `tracks/corretora`):
      · `escopo_*.obrigatorios` — cada id existe em `incisos[].id`?
      · `ordem_exibicao` — bijectivo com conjunto de incisos da matriz?
      · Duplicados, typos, incisos na matriz sem obrigatoriedade nem gatilho (só “fora”)?
2.2 Cruzamento jurídico (humano): cada obrigatório fixo justifica-se na IN 701 para aquela
      modalidade? Registar exceções em nota YAML ou em `MAPEAMENTO_IN701_REF_RES520.txt`.
2.3 Decisão explícita sobre tensão **YAML obrigatórios** vs **remoção por supressão**
      (ids que saem de `active_keys` mas permanecem em `obrigatorios` na ficheiro).
Entregável: tabela obrigatório × trilha × nota de risco.

--------------------------------------------------------------------------------
FASE 3 — Questionários e gatilhos
--------------------------------------------------------------------------------
3.1 `questionnaire_loader.py`: validação na importação; `resolve_triggers` para todos os
      tipos; comportamento de respostas omissas (`normalize_answers`).
3.2 Por trilha, YAML:
      · Cada `when_true`/`when_false`/`add_incisos` referencia id válido na matriz **dessa** trilha?
      · Perguntas `audit_only` e `text_short` não alteram escopo — documentado na UI?
      · Coerência semântica (ex.: “Apenas o cliente” + trânsito Sim): política do produto
        (avisar vs bloquear vs inferir)?
3.3 `potential_triggers_per_inciso` — textos “fora do escopo” fazem sentido para incisos
      só obrigatórios?
Entregável: lista de perguntas ambíguas ou redundantes + proposta de texto/ordem.

--------------------------------------------------------------------------------
FASE 4 — Motor `rules_engine.compute_scope`
--------------------------------------------------------------------------------
4.1 Invariantes a testar (ou provar por teste):
      · `active_keys ⊆ incisos da matriz da trilha`
      · `mandatory ⊆ matriz` e `mandatory_count = |active_keys ∩ mandatory|`
      · `incisos_sujeitos` + `incisos_fora` particionam a matriz
4.2 Ordem de linhas no DataFrame = `sort_scope_keys` (regressão visual/API).
4.3 Duplicação `triggered_by` + obrigatório: `origem_escopo` e texto “por que” consistentes?
Entregável: testes parametrizados `track ∈ TRACK_IDS` para invariantes mínimas.

--------------------------------------------------------------------------------
FASE 5 — `scope_narrative` (supressão e textos)
--------------------------------------------------------------------------------
5.1 `declares_exclusive_non_custodial_model(norm, track)` — casos-limite:
      respostas parciais, tipos errados, listas `P_tp`/`cust_B_tp` mal formadas.
5.2 `suppress_custody_cluster_if_non_custodial` — confirma remoção de `triggered_by` para
      ids suprimidos; impacto em `mandatory_count`.
5.3 `build_why_texts_for_scope` + `merge_llm_whys` — fallback quando LLM falha; não vazar PII.
5.4 Blurbs `P_ARCH` / `CUST_ARCH` — cobrir todas as opções `single_choice` atuais.
Entregável: matriz de cenários × resultado esperado de `active_keys`.

--------------------------------------------------------------------------------
FASE 6 — Jornada 2 e evidências (`evidence_requests.py`)
--------------------------------------------------------------------------------
6.1 `build_journey_2_payload` usa apenas `active_keys` efetivos? Checklist por inciso
      alinhada ao escopo pós-supressão?
6.2 Notas heurísticas (`_journey_2_*_e_notas`) — IDs de pergunta corretos por trilha?
6.3 `AUDITOR_EVIDENCE_BY_INCISO.yaml` — todos os ids possíveis nas três matrizes têm bloco?
      (`missing_evidence_yaml_incisos`)
Entregável: um teste de “inciso suprimido não aparece na checklist J2”.

--------------------------------------------------------------------------------
FASE 7 — Hints BCB e prontidão do corpus
--------------------------------------------------------------------------------
7.1 `bcb_hints_loader` / `BCB_REPORT_HINTS.yaml` — chaves por `inciso_id` e `track` onde
      aplicável; incisos sem hint intencional?
7.2 `readiness.corpus_readiness_for_scope` — não assume trilha errada; mensagens úteis.
Entregável: relatório de incisos sem hint ou com stub.

--------------------------------------------------------------------------------
FASE 8 — API FastAPI e Streamlit
--------------------------------------------------------------------------------
8.1 `main.py` — validação de `track`, tamanho de `answers`, erros 4xx/5xx e mensagens.
8.2 Contrato JSON estável: campos obrigatórios documentados em `SERVIDOR_API.txt`.
8.3 `app.py` — paridade de comportamento com API (mesmo `compute_scope`).
Entregável: diff contrato vs implementação (OpenAPI se existir).

--------------------------------------------------------------------------------
FASE 9 — SPA (`public/` / `vercel_public/`)
--------------------------------------------------------------------------------
9.1 `app.js` — renderização de `origem_escopo`, listas fora do escopo, Jornada 2.
9.2 `decision_tree.js` + `decision_tree.json` — regenerar após mudanças YAML; versão cache.
9.3 Acessibilidade e estados vazios (sem incisos, erro de rede).
Entregável: checklist manual de 3 trilhas × 2 perfis (máximo escopo / mínimo).

--------------------------------------------------------------------------------
FASE 10 — Scripts e ferramentas
--------------------------------------------------------------------------------
10.1 `scripts/validar_projeto.py` — ampliar checagens se Fases 2–4 revelarem gaps.
10.2 `export_decision_tree_data.py` + `sync_vercel_public.py` — documentar no ROTEIRO.
10.3 `simular_empresa.py` — opcional: perfis por trilha ou argumento CLI.
Entregável: um comando CI local (pytest + validar_projeto).

--------------------------------------------------------------------------------
FASE 11 — Suíte de testes
--------------------------------------------------------------------------------
11.1 Cobertura mínima: cada trilha com `compute_scope({})` e `maximize_scope_answers(track)`.
11.2 Cenários de supressão não custodial (já parcialmente em `test_custodiante_scope` /
      `test_rules_engine`).
11.3 Testes de regressão API (`test_api_contract.py`).
11.4 (Opcional) testes de propriedade leves (hypothesis) em `resolve_triggers` com respostas aleatórias
      válidas — nunca id fora da matriz.
Entregável: % cobertura ou lista de funções críticas sem teste.

--------------------------------------------------------------------------------
FASE 12 — Sincronização documentação ↔ código
--------------------------------------------------------------------------------
12.1 `explicação aplicação.txt`, `ROTEIRO_FASES.txt`, `JORNADA_2_*`, `FASE_CORRETORA_*`,
      mapeamentos em `tracks/*` — alinhados ao comportamento real pós-Fases 1–11.
12.2 Remover afirmações obsoletas (ex.: supressão só numa trilha) — grep por palavras-chave.
Entregável: diff de docs ou nota de versão no rodapé dos ficheiros alterados.

--------------------------------------------------------------------------------
FASE 13 — Backlog de melhorias (priorização)
--------------------------------------------------------------------------------
13.1 **P0** — incorreções normativas ou bugs de escopo (dados errados ao cliente).
13.2 **P1** — UX (avisos de incoerência), telemetria de respostas contraditórias.
13.3 **P2** — refatoração (extrair “política de supressão” para tabela declarativa YAML),
      internacionalização, PDF export.
13.4 Cada item: esforço (S/M/L), dono, dependência jurídica (sim/não).

================================================================================
Fim do plano — executar sequencialmente; atualizar este ficheiro com datas/atas se útil.
================================================================================
