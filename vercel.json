{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "python -m pip install --upgrade pip && pip install -r requirements-api.txt",
  "functions": {
    "api/**/*.py": {
      "maxDuration": 60,
      "includeFiles": "laws/**"
    }
  }
}
