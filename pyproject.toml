[project]
name = "certik-vasp-scope"
version = "1.0.0"
description = "IN 701 VASP scoping API + static SPA"
requires-python = ">=3.11"

# Vercel (FastAPI): módulo na raiz evita erro "could not import api/index.py"
[project.scripts]
app = "index:app"
