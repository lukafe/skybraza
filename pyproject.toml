[project]
name = "certik-vasp-scope"
version = "1.0.0"
description = "IN 701 VASP scoping API + static SPA"
requires-python = ">=3.11"

# Vercel: ASGI em api/index.py (pasta api/ = Serverless Functions)
[project.scripts]
app = "api.index:app"
