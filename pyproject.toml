[project]
name = "certik-vasp-scope"
version = "1.0.0"
description = "IN 701 VASP scoping API + static SPA"
requires-python = ">=3.11"

# Vercel FastAPI: main.py na raiz (documentado em vercel.com/docs/frameworks/backend/fastapi)
[project.scripts]
app = "main:app"
